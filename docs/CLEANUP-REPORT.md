# Cleanup report — 2026-08-17

## Deleted

None. No source or asset was deleted without reference evidence.

## Moved for review

None. `_review/` and its manifest are ready for the next evidence-backed pass.

## Kept intentionally

Runtime assets remain untouched because R3F loaders and catalogues use both direct and string paths.

## Generated and ignored

`.next/`, Firebase logs, temporary browser profiles, temporary captures and TypeScript build info are ignored and should not enter Git.

## Human review needed

`realdess-vault/` is empty in this checkout, so its commercial rules cannot yet be copied. GitHub and Firebase require authenticated access before release.
