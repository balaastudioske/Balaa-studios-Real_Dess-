# Assets

Runtime models, wardrobe, merch, props, media and textures are served from `public/` and `public/library/`. Do not relocate assets until direct references, dynamic loader strings and catalogues are validated.
